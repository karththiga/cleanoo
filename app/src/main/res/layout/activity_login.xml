<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/md_green_background"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:importantForAutofill="noExcludeDescendants">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:gravity="center_horizontal">

        <!-- Title -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Welcome Back"
            android:textColor="@color/md_green_primary"
            android:textStyle="bold"
            android:textSize="28sp"
            android:layout_marginTop="40dp"
            android:layout_marginBottom="6dp"/>

        <TextView
            android:id="@+id/tvSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Log in to continue"
            android:textSize="16sp"
            android:textColor="#5F6368"
            android:layout_marginBottom="25dp"/>

        <!-- CardView Container -->
        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="20dp"
            app:cardCornerRadius="18dp"
            app:cardElevation="8dp"
            >

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="22dp">

                <com.google.android.material.tabs.TabLayout
                    android:id="@+id/tabRole"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="18dp"
                    app:tabBackground="@android:color/transparent"
                    app:tabGravity="fill"
                    app:tabMode="fixed">

                    <com.google.android.material.tabs.TabItem
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Household" />

                    <com.google.android.material.tabs.TabItem
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Collector" />
                </com.google.android.material.tabs.TabLayout>

                <!-- Email Field -->
                <EditText
                    android:id="@+id/etEmail"
                    android:layout_width="match_parent"
                    android:layout_height="50dp"
                    android:hint="Email Address"
                    android:padding="14dp"
                    android:inputType="textEmailAddress|textNoSuggestions"
                    android:importantForAutofill="no"
                    android:imeOptions="flagNoPersonalizedLearning"
                    android:autofillHints=""
                    android:privateImeOptions="com.google.android.inputmethod.latin.noSuggestionStrip=true;com.google.android.inputmethod.latin.noPersonalizedLearning=true"
                    android:longClickable="false"
                    android:textIsSelectable="false"
                    android:textColor="#202124"
                    android:textColorHint="#9AA0A6"
                    android:background="@drawable/bg_input_blue"/>

                <!-- Password Field -->
                <EditText
                    android:id="@+id/etPassword"
                    android:layout_width="match_parent"
                    android:layout_height="50dp"
                    android:hint="Password"
                    android:padding="14dp"
                    android:layout_marginTop="16dp"
                    android:inputType="textPassword"
                    android:importantForAutofill="no"
                    android:imeOptions="flagNoPersonalizedLearning"
                    android:autofillHints=""
                    android:privateImeOptions="com.google.android.inputmethod.latin.noSuggestionStrip=true;com.google.android.inputmethod.latin.noPersonalizedLearning=true"
                    android:longClickable="false"
                    android:textIsSelectable="false"
                    android:textColor="#202124"
                    android:textColorHint="#9AA0A6"
                    android:background="@drawable/bg_input_blue"/>

                <!-- Login Button -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnLogin"
                    style="@style/AppPrimaryButton"
                    android:layout_width="match_parent"
                    android:layout_height="50dp"
                    android:text="Login"
                    android:layout_marginTop="24dp"
                    android:textAllCaps="false"
                    android:textColor="#FFFFFF"
                    android:textSize="17sp"
                    android:backgroundTint="@color/md_green_primary" />

                <!-- Forgot Password -->
                <TextView
                    android:id="@+id/tvForgotPassword"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Forgot Password?"
                    android:textColor="@color/md_green_primary"
                    android:textStyle="bold"
                    android:layout_gravity="center_horizontal"
                    android:layout_marginTop="18dp"/>

            </LinearLayout>

        </androidx.cardview.widget.CardView>

        <!-- Signup Redirect -->
        <TextView
            android:id="@+id/tvSignup"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="New here? Create an account"
            android:textColor="@color/md_green_primary"
            android:textStyle="bold"
            android:layout_marginBottom="30dp"/>

    </LinearLayout>

</ScrollView>
